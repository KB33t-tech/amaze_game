package others;

/**
 * 
 * This is an enumeration class that holds the states of the game.
 * It is used in the switch statement inside {@link ChangeState}.
 *
 */
public enum State {

	START_SCREEN, INSTRUCTION, GAME, WIN, LOSE;
	
}
